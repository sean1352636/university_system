# Education System — Pinned RUNTIME Lock
# =======================================
# Exact, reproducible pins for the DEFAULT (runtime) install only. The minimum
# compatible ranges live in pyproject.toml ([project] dependencies).
#
#   pip install -r requirements.txt        # reproducible runtime install
#   pip install -e .                       # range-based install from pyproject
#
# Development / CI tooling (pytest, ruff, mypy, bandit, locust, …) is NOT in
# this file — it lives in requirements-dev.txt so ordinary users don't pull it.
#
# Optional feature tiers (cloud, graphql, realtime, alt DB backends, LMS
# integrations, deep-learning AI) are commented out below and are installed via
# the matching pip extra, e.g.  pip install .[cloud-aws]   pip install .[graphql]
#
# To refresh pins:  pip freeze > requirements.txt  (then re-tidy the sections)

# ===============================
# DATA PROCESSING & ANALYTICS
# (core: imported across academics & finance domains)
# ===============================
pandas==2.3.3                  # Data manipulation and analysis
numpy==2.4.4                   # Numerical computing
matplotlib==3.10.8             # Data visualization and plotting
seaborn==0.13.2                # Statistical data visualization
scikit-learn==1.8.0            # Machine learning algorithms
scipy==1.17.1                  # Scientific computing and statistics
joblib==1.5.3                  # Parallel computing / model persistence (scikit-learn dep)

# ===============================
# DOCUMENT GENERATION & REPORTS
# ===============================
reportlab==4.4.10              # PDF generation and reports
openpyxl==3.1.5                # Excel file handling and export
python-docx==1.2.0             # Word document support
pypdf==6.14.2                  # PDF processing (replaces abandoned PyPDF2; >=6.10.2/6.12.0 fix FlateDecode / xref / layout-mode DoS CVEs — Dependabot #26-#29, #36-#37; >=6.13.3/6.14.0/6.14.1/6.14.2 fix further parsing DoS CVEs — alerts #59, #74-77)

# ===============================
# WEB & NETWORKING
# ===============================
requests==2.33.1               # HTTP library for API calls
Flask==3.1.3                   # Web framework
flask-cors==6.0.2              # CORS support for Flask APIs
Jinja2==3.1.6                  # Template engine for email templates
Werkzeug==3.1.8                # WSGI utilities (Flask dep)
urllib3==2.7.0                 # HTTP client — >=2.7.0 fixes decompression-bomb bypass & cross-origin header forwarding (Dependabot #32-#33)
pydantic==2.12.5               # Data validation and schema definition

# ===============================
# DATABASE & MIGRATIONS
# (SQLite is built-in; Postgres/MySQL are optional extras — see below)
# ===============================
alembic==1.18.4                # Schema migrations
SQLAlchemy==2.0.49             # Required by alembic
Mako==1.3.12                   # Required by alembic; >=1.3.12 fixes CVE-2026-44307 (GHSA-2h4p-vjrc-8xpq)

# ===============================
# SECURITY & AUTHENTICATION
# ===============================
pyotp==2.9.0                   # Two-factor authentication (TOTP)
qrcode==8.2                    # QR code generation for 2FA
cryptography==48.0.1           # Cryptographic functions (>=46.0.7 fixes GHSA buffer-overflow on non-contiguous buffers; >=48.0.1 fixes further advisory — Dependabot alert #52)
bcrypt==5.0.0                  # Password hashing
PyJWT==2.13.0                  # JSON Web Token authentication (>=2.13.0 fixes CVEs — Dependabot alerts #38-42)
defusedxml>=0.7.1              # Safe XML parsing (prevents XXE attacks)

# ===============================
# IMAGE PROCESSING & BARCODES
# ===============================
Pillow>=12.2.0                 # Image processing library
python-barcode==0.16.1         # Barcode generation for library cards

# ===============================
# SYSTEM UTILITIES & SCHEDULING
# ===============================
schedule==1.2.2                # Task scheduling and automation
croniter==6.2.2                # Cron expression parsing for scheduled tasks
psutil==7.2.2                  # System monitoring and process management

# ===============================
# DATE/TIME & CONFIGURATION
# ===============================
pytz==2025.2                   # Timezone handling
PyYAML==6.0.3                  # YAML configuration files
python-dateutil==2.9.0.post0   # Date parsing and manipulation
python-dotenv==1.2.2           # Environment variable management

# ===============================
# LOGGING & OBSERVABILITY
# ===============================
python-json-logger>=2.0.0      # Structured JSON logging (ELK / OpenSearch compatible)
colorama==0.4.6                # Cross-platform colored terminal text
colorlog==6.10.1               # Colored logging output

# ===============================
# EMAIL & COMMUNICATIONS
# (smtplib / email are built into Python)
# ===============================
email-validator==2.3.0         # Email address validation

# ===============================
# CALENDAR & SCHEDULING EXTRAS
# ===============================
ics==0.7.2                     # ICS/iCalendar file generation and parsing
icalendar==7.0.3               # iCalendar file handling and parsing
holidays==0.94                 # Holiday calendar and date checking

# ===============================
# TEXT PROCESSING & NLP (basic)
# ===============================
nltk==3.10.0                   # Natural language processing toolkit (>=3.10.0 fixes advisory — Dependabot alert #58)
langdetect==1.0.9              # Language detection for text
fuzzywuzzy==0.18.0             # Fuzzy string matching
python-Levenshtein==0.27.3     # Fast string matching (speedup for fuzzywuzzy)

# ===============================
# GUI (Tkinter add-ons; tkinter ships with Python)
# ===============================
# If tkinter is missing:  sudo apt-get install python3-tk   (Ubuntu/Debian)
ttkthemes==3.3.0               # Additional themes for Tkinter
tkcalendar==1.6.1              # Calendar widget for Tkinter

# ===============================
# ADDITIONAL UTILITIES
# ===============================
tqdm==4.67.3                   # Progress bars for loops
tabulate==0.10.0               # Pretty-print tabular data
jsonschema==4.26.0             # JSON schema validation
chardet>=5.1.0                 # Character encoding detection
certifi==2026.2.25             # SSL certificate bundle
cachetools==7.0.5              # Caching utilities

# =============================================================================
# OPTIONAL FEATURE TIERS — install the matching pip extra instead of
# uncommenting here, e.g.  pip install .[graphql]   pip install .[cloud-aws]
# The pins are kept for reference / reproducible optional installs.
# =============================================================================

# --- Interactive Plotly dashboards:  pip install .[viz] ---
# plotly==6.7.0

# --- GraphQL API layer:  pip install .[graphql] ---
# strawberry-graphql[flask]>=0.215.0

# --- Realtime / WebSocket:  pip install .[realtime] ---
# flask-socketio>=5.3.0
# python-socketio>=5.10.0
# eventlet>=0.36.0
# aiohttp==3.14.1              # async HTTP; pin retained for the CVE-2026-345xx batch & alerts #34-#35

# --- Alternative database backends:  pip install .[postgres] / .[mysql] ---
# psycopg2-binary==2.9.11
# PyMySQL==1.1.2

# --- Cloud storage (install only the provider you use):  .[cloud-aws] etc. ---
# boto3==1.42.76
# azure-storage-blob>=12.0.0
# google-cloud-storage>=2.0.0

# --- External integrations (LMS, SMS):  pip install .[integrations] ---
# google-api-python-client>=2.100.0
# google-auth>=2.23.0
# google-auth-oauthlib>=1.1.0
# twilio>=8.0.0

# --- Remote operations (SSH/SFTP):  pip install .[remote] ---
# paramiko==4.0.0

# --- Deep-learning AI / advanced NLP:  pip install .[ai]  (choose torch OR tensorflow) ---
# torch>=2.0.0
# transformers>=4.30.0
# spacy>=3.6.0
# opencv-python>=4.5.0

# --- Transitive security override (uncomment if a dep pulls mcp) ---
# mcp>=1.23.0                  # Fix GHSA-9h52-p55h-vw2f

# ===============================
# SYSTEM DEPENDENCIES (OS packages, not pip)
# ===============================
#   Ubuntu/Debian: sudo apt-get install python3-tk python3-dev libpq-dev
#   CentOS/RHEL:   yum install tkinter python3-devel postgresql-devel
#   macOS:         brew install postgresql
#
# REQUIREMENTS: Python 3.11+  •  2GB RAM min (8GB for [ai])  •  500MB disk
