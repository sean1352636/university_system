"""Compatibility shim for the ``university_system.extensions`` namespace.

The target package ``education_system.university_system.modules.extensions``
does not exist yet. This stub prevents ImportError for code that references
the old path — it simply exposes an empty namespace until the package is created.
"""
