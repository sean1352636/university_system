"""Campus events — discovery, RSVP, reminders.

Relocated from ``modules.domain.student_affairs.events`` into student_affairs as part of
the domain-consolidation pass. The implementation is unchanged.
"""
